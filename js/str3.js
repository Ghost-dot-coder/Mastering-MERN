// wap to print given string starts with Q
const prompt = require("prompt-sync")({ sigint: true });
const data = prompt("Enter a Word:").toUpperCase();
console.log(data.startsWith("Q"));

// Wap to print all vowels from the given string
const input = "hello";
var vowels = console.log();
